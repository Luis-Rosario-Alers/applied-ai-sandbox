# M5 — Task Decomposition

## Decomposition (8-10 tasks)

1. **Task 1 — Extend note dictionary to include tags key (a list of strings, empty by default)**: _Verify: Existing test still pass and a new note contains `tags == []`._
2. **Task 2 — Update note form to accept comma seperated tags field**: _Verify: Create a note with the tags `personal, work`_:
3. **Task 3 — Ensure note form can accept zero tags** _Verify: Create a note with no tags_:
4. **Task 4 — Update notes list to show tags as chips next to each note title**: _Verify: Look at notes list and check for tags next to note titles_
5. **Task 5 — Update notes list to include a filter based on tags for each note**: _Verify: Use filter to list notes that have the `personal` tag_
6. **Task 6 — Create test for proper tag parsing in note form**: _Verify: test properly covers general test cases and uncommon edge cases_
7. **Task 7 — Create test for proper tag filtering behaviour**: _Verify: test properly covers general test cases and uncommon edge cases_



## Task 1 context brief

### Goal
Extend note dictionary object to include a `tags` key that holds an empty string array

### Constraints
- Do not modify any environment configuration, build configurations, or git tracking files (like .gitignore or requirements.txt).
- Focus strictly on internal data structure modifications within the application logic.
- Do not add any UI components or frontend templates yet; this task is backend-only.

### Files to be changed
`app.py` - Utilizes notes dict to load notes list and creates new notes

### Acceptance
- New notes dict object contains tags key with default value being an empty string list
- All existing tests pass
- A new note should contain `tags == []`

Do not start writing code immediately, you must plan your approach first.

## Task 1 — what happened

- Claude's plan summary: Update the notes append call to append a note dict obj with a tags key that has an empty list as its value by default
- What I pushed back on: Nothing, Claude was able to detect that it was a pretty simple change and anything obvious past that was not in scope anyway.
- Verification result: No errors or regressions were introduced.

## Task 2 — what happened
- Claude's plan summary: Update notes form with an optional tags field and implement back-end logic to handle tags input from notes form.
- What I pushed back on: Nothing
- Verification result: All existing tests pass and creating a note with the tags `personal, work` works.
  
## Task 3 — what happened
- Claude's plan summary: Update notes form to allow empty tags field
- What I pushed back on: Nothing, it seems that task 2 already completed 
- Verification result: All existing tests pass and a note without tags can be created.

## Reflection — where I redirected

- There wasnt a task where the plan came back wrong the first try because I made each task incredibly straightforward
- The brief for task 2 borrowed lessons from task 1 by better optimizing contraints
- One thing I would change about my decomposition would be having the splitting of tasks to be slightly more atomic to avoid cases where the completion of one task essentially completes another.
