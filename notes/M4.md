# M4 — Reading and Verifying Output

## Bug hunt #1 — SQL injection
- Bug: SQL injection
- Why Claude wrote it: Claude wrote it because its the most straightforward way to introduce SQL queries in the application and the code generated seems to follow idiomatic python code techniques.
- Fix: Use modern ORM technology to avoid executing RAW queries that can be directly modified by user input.

## Bug hunt #2 — hallucinated package
- Bug: The package `flask_simpleauth` does not exist.
- Why Claude wrote it: Claude wrote it because the package name resembles a real package called `Flask-Login`. 
- Fix: Replace `flask_simpleauth` with the real package, `Flask-Login`

## Bug hunt #3 — edge case
- Bug: Division By zero
- Why Claude wrote it: Claude wrote it because without extensive testing, the test case seems to work for most code.
- Fix: Add additional logic to detect if the ratings list is empty.

## Bug hunt #4 — hardcoded secret
- Bug: Hardcoded API Key
- Why Claude wrote it: Claude wrote it because it most likely didnt have constraint on if it could use hardcoded API keys within the codebase.
- Fix: Convert the hardcoded API key into an environment variable that represents the actual API key

## Test I wrote
- Bug I picked: Division By Zero
- Test code:

```python
@pytest.mark.parametrize("input_list, expected_output", [
    ([4, 5, 3], 4.0),    
    ([1, 2], 1.5),       
    ([], None),          
])
def test_average_rating_edge(input_list, expected_output):
    assert average_rating(input_list) == expected_output
```

- Confirmed it fails on the bug and passes on the fix? Yes.

## Near-miss reflection
- Claude produced code for my Blog Post website that seemed correct on the surface. However, the contrast between my use of libraries and claudes was very apparent. After closer inspection, I found out that claude was using a markdown js library that didnt even exist. I likely would've caught this earlier if I verified the libraries that my AI would import to my projects.
