# M3 — Context Engineering

## Step 2 — CLAUDE.md behaviours

- Claude will try to keep the scope as limited as possible, only modifying files needed to complete the goal
- Claude will modify files based on the behaviour the respective test file expects
- Claude will only consider a task "complete" if the `test_task_NN.py` file passes
